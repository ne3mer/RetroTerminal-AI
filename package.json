{
  "name": "retro-terminal-ai-chat",
  "version": "1.0.0",
  "description": "1980s themed terminal AI chat application",
  "main": "server.js",
  "scripts": {
   "dev": "nodemon --no-deprecation server.js",
  "start": "node --no-deprecation server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build"
  },
  "dependencies": {
    "axios": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "nodemon": "^3.0.1",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.3.3"
  },
  "engines": {
    "node": "18.x"
  }
}
